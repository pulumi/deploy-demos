name: static-site
runtime: nodejs
description: A static website
config:
  # pulumi-aws v7 requires an explicit region on fresh stacks. The drivers create stacks
  # under generated names, so Pulumi.dev.yaml does not apply to them — default it at the
  # project level.
  aws:region:
    value: us-west-2
