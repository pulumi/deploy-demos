name: bucket-time-yaml
runtime: yaml
description: Minimal single-S3-bucket benchmark fixture (YAML)

resources:
  # Logical name avoids '-': YAML expressions like ${name.id} parse '-' as subtraction.
  bucketTimeBucket:
    type: aws:s3:Bucket

outputs:
  bucketName: ${bucketTimeBucket.id}
